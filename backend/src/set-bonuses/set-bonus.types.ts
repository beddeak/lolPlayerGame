export interface SetBonusSnapshot {
  id: number;
  code: string;
  name: string;
  chemistryBonus: number;
  laningBonus: number;
  teamFightBonus: number;
  macroBonus: number;
  teamPlayBonus: number;
}
